# To-Do List

A simple to-do list application built with HTML, CSS, and vanilla JavaScript, using `localStorage` for browser-based data persistence.

## How to Run

1. Clone the repository or download the files.
2. Open the `index.html` file in any modern web browser.
